import makeStyles from "@material-ui/core/styles/makeStyles";

export const useStyles = makeStyles({
    root: {
        minWidth: 275,
        minHeight: 350,
        overflow: 'auto',

    },
    textField: {
        margin: 8,
    },
})